export const ADDON_ID = 'storybook-addon-uizze';
export const PANEL_ID = `${ADDON_ID}/panel`;
export const PARAM_KEY = 'uizze';
export const DEFAULT_REQUIRED_STATES = ['ready', 'loading', 'empty', 'error'] as const;
